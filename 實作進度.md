# 志願分組（Preference-Based Grouping）實作進度

*最後更新：2026-09-26*

## 目前狀態

核心功能都做完了：三種填寫模式（押注渴望度／三級渴望度／第一志願＋可接受）、分配演算法（含放寬到最少人數、抽籤／讓位事件記錄）、主辦方後台（建立、編輯、已填寫名單、執行分組、分組明細）、組員填寫頁（依模式切換不同 UI）、公正性設計（主辦方看不到志願內容、亂數 SHA-256 公開驗證）、Vercel 淺色配色＋動畫、手機/電腦模式分流的骨架（目前兩邊長得一樣，等實際需要分岔時再改）。今天用 `code-review` skill 掃過全專案，抓到並修好 3 個真實 bug（見「最新完成」），新增 `preflight-check.mjs` 健檢腳本 + `docs/checklist-code-review.md` 人工複查清單。還沒做的大項目：組員可接受志願的「拖曳插槽」互動改版、Vercel 部署保護造成組員被要求登入、主辦方帳號系統（Google 登入 + 我的活動列表）——這三項待使用者決定後才動工，見「待辦」。

## 最新完成

> 歷史紀錄：[docs/history/completed.md](docs/history/completed.md)（超過 8 筆自動歸檔，保留最新 5 筆，見 [docs/archive-done.mjs](docs/archive-done.mjs)）

| 功能 | 關鍵檔案 | 摘要 |
|------|---------|------|
| v0.1 — 建立活動、志願＋渴望度填寫、截止後自動分配 | `src/lib/assign.ts`（新）、`src/app/api/activities/**`、`src/app/a/[id]/**` | 逐輪志願＋可行性檢查的分配演算法（保證前 ⌈m/2⌉ 志願）、Neon Postgres／本機 JSON 雙資料層、QR code 邀請、懶結算（截止後第一個請求觸發）。 |
| 渴望度總數改為 ⌈m×3÷2⌉、無解時只放寬最少人數、「最多 N 人」改稱人數上限 | `src/lib/assign.ts` | 原本 100 點固定預算改依職位數計算；無解時舊版全體放寬，改成用 seed 隨機挑出理論最小人數放寬。 |
| 套用作品集淺色配色與動畫特效 | `src/app/globals.css`、`src/components/motion.tsx`、`src/components/overscroll-bounce.tsx` | 從 `C:\Portfolio` 復刻暖色調配色（`#FBF3DC`/`#EFE5C8`/`#7E6725`）+ framer-motion 卡片淡入/按鈕彈跳/邊緣回彈效果。分頁圖示換成作品集提供的綿羊圖片。 |
| 主辦方可在截止前編輯活動、資料庫錯誤訊息明確化 | `src/lib/activity-input.ts`（新）、`src/app/api/activities/[id]/host/route.ts` | `PATCH` 支援修改名稱/敘述/截止時間/職位/人數上限；結構性變更需確認清空填寫。未連資料庫時回傳可讀訊息而非籠統的「伺服器錯誤」。 |
| 主辦方後台結算後顯示分組明細與分配過程紀錄 | `src/lib/service.ts`（`hostDetail`）、`src/components/host-detail.tsx`（新） | 每人分到第幾志願＋押注、抽籤（tie）／讓位（yield）／降低標準（relax）事件列表，不洩漏其他志願排序。 |
| 三種填寫模式＋主辦方手動分組、即時填寫名單、倒數緊急標籤 | `src/lib/assign.ts`（志願序廣義化成「順位」）、`src/lib/modes.ts`（新）、`src/lib/service.ts`（`finalizeNow`/`hostSubmissions`） | 新增三級渴望度／第一志願＋可接受兩種模式；移除自動結算，改成主辦方手動按「執行分組」；後台隨時看得到已填寫名單；截止時間 3 天/1 天/1 小時內紅色倒數標籤。 |
| 手機／電腦模式先分流（目前兩邊長得一樣） | `src/lib/viewport.ts`（新）、`src/app/page.tsx`／`a/[id]/page.tsx`／`a/[id]/host/page.tsx` | `useViewport()` 依 768px 斷點分流到 `Mobile*Page`/`Desktop*Page`，兩者目前都呼叫同一個 `*PageCore`，先建立分岔點供之後個別調整。 |
| code-review 抓到並修好 3 個真實 bug + 新增 preflight 腳本與複查清單 | `src/lib/assign.ts`、`src/lib/activity-input.ts`、`preflight-check.mjs`（新）、`docs/checklist-code-review.md`（新） | ① pick 模式多職位同順位的搶奪沒記錄成事件 ② 編輯活動時未修改的截止時間被誤擋 ③ 同順位多職位的隨機選擇其實是整場固定值不是每次重抽。三個都已修復並補回歸測試（26→30 案）。 |

## 待辦

- [ ] 使用者決定：組員可接受志願的互動方式從「單一可排序清單」改成「志願插槽」（固定數量的插槽，把職位卡片拖進插槽、有磁吸吸附效果），保留「以下原則上不會被分到」那條分界線——待使用者確認規格後再實作（見計畫文件）
- [ ] 使用者處理：組員透過邀請連結進入時被要求登入 Vercel——這是 Vercel 專案的 Deployment Protection 設定造成的，不是程式碼問題，需要使用者自己到 Vercel 後台調整
- [ ] 使用者決定：主辦方帳號系統（首頁改成標題+登入介面、「建立活動」按鈕未登入時觸發 Google 登入、旁邊加「查看已建立的活動」按鈕列出進行中/已結束的活動）——這是新的資料模型（活動要跟帳號綁定），待使用者確認規格後建立計畫再實作
- [ ] 使用者部署動作：確認 Vercel 專案已連接 Neon 資料庫（`/api/health` 應顯示 `"database": "postgres"`）
- [ ] Q3（v0.1 就留著的問題）：要不要讓主辦方預先輸入組員名單，才能顯示「誰還沒填」（目前只能顯示「誰已經填了」）

## 相關文件

- [docs/plan-rolematch.md](docs/plan-rolematch.md) — 完整實作計畫，含每個版本的設計細節與跟原計畫的差異
- [docs/checklist-code-review.md](docs/checklist-code-review.md) — 人工複查清單，跟 `preflight-check.mjs` 互補
- [docs/開發血汗錄.md](docs/開發血汗錄.md) — 多輪排查才抓到根因的除錯記錄（目前是空範本）
